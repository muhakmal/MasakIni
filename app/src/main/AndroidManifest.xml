<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baskom.masakini">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_mi"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".splash.WelcomeActivity" />
        <activity android:name=".userarea.LoginActivity" />
        <activity android:name=".userarea.BuatAkunActivity" />
        <activity android:name=".subactivity.drawer.InfoAkunActivity"/>
        <activity android:name=".subactivity.drawer.RiwayatOrderActivity"/>
        <activity android:name=".subactivity.CartActivity"/>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>