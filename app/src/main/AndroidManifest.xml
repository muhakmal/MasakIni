<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.baskom.masakini">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_mi"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity
            android:name=".view.SplashActivity"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.WelcomeActivity" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.BuatAkunActivity" />
        <activity android:name=".view.InfoAkunActivity" />
        <activity android:name=".view.RiwayatOrderActivity" />
        <activity android:name=".view.TroliActivity" />
        <activity android:name=".view.ResepDetilActivity" />
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.PembelianKonfirmasiActivity" />
        <activity android:name=".view.PembelianSelesaiActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".view.CartActivity"></activity>
    </application>

</manifest>